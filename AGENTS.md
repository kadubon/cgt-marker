Use neutral and scientific reasoning.

This repository implements a small contradiction-marker state layer inspired by CGT. Keep the
package practical, deterministic, framework-neutral, and easy to port to other languages.

Do not turn the library into a theorem prover, belief-revision system, memory system, or agent
framework. The core principle is: mark contradictions, preserve claims and provenance, and let
callers continue with unresolved markers visible.
